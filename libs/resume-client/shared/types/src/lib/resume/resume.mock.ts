import { mockMain } from '../main/main.mock';
import { mockSidebar } from '../sidebar/sidebar.mock';

import { Resume } from './resume';

export const mockResume: Resume = {
  main: mockMain,
  sidebar: mockSidebar
};
